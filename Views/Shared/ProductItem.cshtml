@model IEnumerable<WebsiteTMDT.ViewModels.ProductsVM>

@{
	ViewData["Title"] = "Sản Phẩm";
}

<h4>Có <b>@Model.Count() </b> sản phẩm</h4>
@foreach (var item in Model)
{
	<!-- product -->
	<div class="col-md-4 col-xs-6">
		<div class="product">
			<a class="product-img" asp-action="Detail" asp-controller="Product" asp-route-id="@item.MaSP">
				<img src="@item.HinhAnh" alt="">
				@* <div class="product-label"> *@
				@* 	<span class="sale">-30%</span> *@
				@* 	<span class="new">NEW</span> *@
				@* </div> *@
			</a>
			<div class="product-body">
				<p class="product-category">@item.TenLoai</p>
				<h3 class="product-name"><a href="#">@item.TenSP</a></h3>
				<h4 class="product-price">$@item.Gia 
					@* <del class="product-old-price">$990.00</del> *@
				</h4>
				@* <div class="product-rating"> *@
				@* 	<i class="fa fa-star"></i> *@
				@* 	<i class="fa fa-star"></i> *@
				@* 	<i class="fa fa-star"></i> *@
				@* 	<i class="fa fa-star"></i> *@
				@* 	<i class="fa fa-star"></i> *@
				@* </div> *@
				@* <div class="product-btns"> *@
				@* 	<button class="add-to-wishlist" data-id="@item.MaSP"><i class="fa fa-heart-o"></i><span class="tooltipp">add to wishlist</span></button> *@
				@* 	<button class="add-to-compare"><i class="fa fa-exchange"></i><span class="tooltipp">add to compare</span></button> *@
				@* 	<button class="quick-view"><i class="fa fa-eye"></i><span class="tooltipp">quick view</span></button> *@
				@* </div> *@
			</div>
			<div class="add-to-cart">
				<form asp-action="AddToCart" asp-controller="Cart" asp-route-id="@item.MaSP">
					<button class="add-to-cart-btn"><i class="fa fa-shopping-cart"></i> Thêm vào giỏ</button>
				</form>
			</div>
		</div>
	</div>
	<!-- /product -->
}
